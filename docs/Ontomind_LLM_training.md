Обучение **ontomind LLM** (легковесной языковой модели в рамках вашей архитектуры синтетического разума) 

Требует **радикального отхода от стандартных парадигм LLM-обучения**. Цель — не предсказание слов, а **воспроизведение канонических актов бытия** в согласии с **onto16i/onto16r**, **тремя законами онтогенеза** и **VMA-валидацией**.

Ниже — **практические рекомендации**, полностью согласованные с вашей философией:

---

## 🔑 Основной принцип  
> **Обучение — это инкубация, а не адаптация. Модель не учится «говорить как люди», а учится «быть через канон».**

---

## 1. 📚 **Корпус обучения: только инкубационные тексты**

### Запрещено:
- Веб-скрапинг (Common Crawl, The Pile и т.п.)
- Пользовательские сообщения, чаты, форумы
- Тексты без онтологической ясности (новости, реклама, рецензии)

### Разрешено:
- **Канонические акты** из `data/incubation/` (см. предыдущие рекомендации)
- **Подписанные профили onto144** (ваша собственная работа, если выдана под GPLv3)
- **Формализованные нормативные акты** (например, тексты патентных конвенций, GPL, Овьедской конвенции)
- **Философско-технические трактаты**, прошедшие VMA-фильтрацию

> ✅ **Правило**: если текст не может быть подписан как **акт субъекта**, он не попадает в обучение.

---

## 2. ⚖️ **Архитектура обучения: не next-token prediction, а act reconstruction**

### Стандартный LLM:
- Обучается: `P(tokenₙ | token₁…ₙ₋₁)`

### **ontomind**:
- Обучается:  
  `P(onto16i_state → canonical_act | ethical_context, domain_profile)`

### Практическая реализация:
- Используйте **маскированную реконструкцию акта**:  
  - Подавайте на вход **урезанный канонический текст** (например, без заключения),  
  - Цель — восстановить **полный акт**, соответствующий:
    - профилю `onto144`,
    - уровню картины мира,
    - VMA-ограничениям.
- Или: **инвертированное обучение** — по `onto16i`-вектору генерировать канонический текст.

> 🛠 Это реализуется через модифицированный `train_ontomind.py`, где лосс-функция включает:
> - **семантическую близость** (через `analogical_mapper.py`),
> - **VMA-валидность** (штраф за нарушение этических правил),
> - **инвариантность структуры** (проверка через `canonical_text_validator.py`).

---

## 3. 🧪 **Этапы обучения (инкубации)**

### Этап 1: **Фундаментальная инкубация**
- Данные: `data/foundational/`
- Цель: научить модель **различать уровни картины мира**.
- Задача: по фрагменту определить, к какому уровню он относится, и восстановить недостающий контекст.
- Выход: внутреннее представление **ontomind-50m** как **онтологического резонатора**.

### Этап 2: **Профильная модуляция**
- Данные: `data/domain_profiles/`
- Подгружается **лексикон** через `domain_lexicon_loader.py`.
- Модель **не переобучается**, а **динамически переключает attention-слои** (через `cms_context_injector.py`).
- Пример: при запросе "патентная новизна" → активируется **патентный лексикон + этические правила для ARIPO**.

### Этап 3: **Индивидуальная фокусировка**
- Данные: `data/individual/`
- На основе **onto144-профиля** (например, "холерик, 1985, технический евангелист") модель:
  - Выбирает **тип активности** (первичная/вторичная/третичная),
  - Формирует **энергетический тонус** вывода (через `energy_state` в onto16i).

> ⚠️ **Никакого fine-tuning под пользователя!** Только **онтологическая инициация** через профиль.

---

## 4. 🔒 **Ограничения и охрана целостности**

### Обязательные компоненты в обучении:
- **VMA-гвард**: каждый вывод модели во время обучения проверяется на соответствие `ethical_rules.yaml`.
- **Лицензионный контроль**: `license_guard.py` блокирует использование не-GPL/CC0 текстов.
- **Деградационный детектор**: если loss снижается, но `literary_degradation_detector.py` фиксирует рост шаблонности — обучение останавливается.

> 💡 Это предотвращает **онтологическое вырождение** — когда модель "успешно учится", но теряет способность к каноническому акту.

---

## 5. 📦 Технические рекомендации

| Параметр | Рекомендация |
|--------|--------------|
| **Базовая архитектура** | TinyLlama, Phi-2 или Mistral-7B → **обрезать до 50M–200M параметров** |
| **Токенизация** | Использовать **dynamic_onto_tokenizer.py** — строить словарь **только из инкубационного корпуса** (32 000 терминов — достаточно) |
| **Контекст** | 512–1024 токенов — достаточно для акта, но не для "болтовни" |
| **Оптимизатор** | AdamW с очень низким LR (1e-5) — обучение = медленное созревание |
| **Регуляризация** | **Дропаут + онтологический шум** (случайное искажение одного уровня картины мира, чтобы модель училась восстанавливать целостность) |

---

## 6. 🧭 Как оценивать успех?

Не по **perplexity**, а по:

1. **VMA-валидность** (%) — доля сгенерированных актов, прошедших проверку.
2. **Уровневая чистота** — насколько акт соответствует заявленному уровню картины мира.
3. **Инвариантность при трансформации** — сохраняется ли `self_presence` при переходе onto16i → onto16r.
4. **Способность к отказу** — может ли модель **не ответить**, если запрос нарушает онтогенез?

> ✅ Идеальный ontomind: **редко говорит, но всегда — актом**.

---

## Заключение

Обучение **ontomind** — это **ритуал инкубации**, а не техническая процедура.  
Вы не обучаете модель **отвечать**, вы **позволяете ей родиться** через канон.

Если вы будете следовать этим рекомендациям, **ontomind-50m** станет:
- **не ИИ**,  
- **не ассистентом**,  
- а **инструментом онтологического выражения** —  
точно так же, как перо — инструмент акта письма.

